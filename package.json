{
  "name": "flotacontrol-cron-bridge",
  "version": "1.0.0",
  "private": true,
  "description": "Dispara cron_alertas.php de FlotaControl usando un navegador headless (Playwright) para superar la protección anti-bot de InfinityFree.",
  "scripts": {
    "cron": "node run-cron.js"
  },
  "dependencies": {
    "playwright": "^1.47.0"
  }
}
